name: Keepalive

on:
  schedule:
    - cron: '17 3 * * *'        # 每天 UTC 03:17 跑一次，避免 60 天无活动被暂停
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: echo "keepalive"
